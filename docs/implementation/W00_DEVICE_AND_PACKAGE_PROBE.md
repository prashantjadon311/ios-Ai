# W00 — Device, Toolchain and Package Probe

**gate_id:** W00  
**date:** 2026-09-25  
**environment_OS:** Linux Ubuntu 7.0.0-31-generic x86_64  
**toolchain_version:** Swift: NOT_FOUND; Xcode: NOT_FOUND; iOS SDK: NOT_AVAILABLE  
**status:** PARTIAL — template PRESENT, iOS compile BLOCKED

---

## 1. Toolchain probe

| Tool | Result |
|---|---|
| `swift --version` | command not found |
| `swiftc` | command not found |
| `xcodebuild` | command not found |
| OS | Linux x86_64 (not macOS/iOS) |

**iOS-framework compilation and physical-device validation: NOT_RUN** per spec 12 §Platform.

---

## 2. Exported iPad Swift Playgrounds template

A genuine Playgrounds-generated `.swiftpm` exists at `PersonalAssistant.swiftpm/`.

**Package.swift inspection:**

```
swift-tools-version: 5.9
Package name: PersonalAssistant
Platform: .iOS("18.6")
Product: .iOSApplication(name: "PersonalAssistant", targets: ["AppModule"])
Target: .executableTarget(name: "AppModule", path: ".")
DisplayVersion: "1.0", bundleVersion: "1"
AppIcon: .placeholder(icon: .smiley)
AccentColor: .presetColor(.brown)
SupportedDeviceFamilies: [.pad, .phone]
Orientations: portrait, landscapeRight, landscapeLeft, portraitUpsideDown(pad only)
```

**Playgrounds workspace files:**
- `.swiftpm/playgrounds/CachedManifest.plist` — present
- `.swiftpm/playgrounds/DocumentThumbnail.plist` — present
- `.swiftpm/playgrounds/DocumentThumbnail.png` — present
- `.swiftpm/playgrounds/Workspace.plist` — present

**App target source path:** `.` (all `.swift` files in `PersonalAssistant.swiftpm/`)

**Conclusion:** Genuine iPad Playground template confirmed. Package structure intact. iPad import path NOT_RUN (no device).

---

## 3. Framework availability probes

Frameworks cannot be compiled/probed on Linux. Availability status per V3 doc 17:

| Framework | V1 Required? | Status on Linux |
|---|---|---|
| SwiftUI | Yes | NOT_RUN (no iOS SDK) |
| SwiftData | Yes (persistence) | NOT_RUN |
| AVFoundation | Yes (voice/audio) | NOT_RUN |
| Speech (SFSpeechRecognizer) | Yes (STT) | NOT_RUN |
| AVSpeechSynthesizer | Yes (TTS) | NOT_RUN |
| UserNotifications | Yes (reminders) | NOT_RUN |
| URLSession | Yes (HTTP/SSE) | NOT_RUN |
| Security (Keychain) | Yes | NOT_RUN |
| PhotosUI | V1 (attachments) | NOT_RUN |
| LocalAuthentication | V1 (biometric) | NOT_RUN |
| CoreSpotlight | COND | NOT_RUN |
| FoundationModels | COND | NOT_RUN |
| Vision (OCR) | COND | NOT_RUN |
| BackgroundTasks | COND | NOT_RUN |

---

## 4. Deployment target

- **Proposed iOS/iPadOS floor:** 18.6 (as generated by Playgrounds)
- **Swift tools version:** 5.9 (as declared in Package.swift)
- All availability-gated APIs must use `#available(iOS X, *)` guards per spec A18/I11.

---

## 5. Capability declarations

Capabilities to be declared in Package.swift (when Xcode project wrapper is created) or Info.plist:
- `NSMicrophoneUsageDescription` — Voice input
- `NSSpeechRecognitionUsageDescription` — STT
- `NSPhotoLibraryUsageDescription` — Attachments
- `NSCameraUsageDescription` — Future (not V1)
- `NSUserNotificationsUsageDescription` — Task reminders
- `NSContactsUsageDescription` — Contacts tool (COND)
- `NSCalendarsUsageDescription` — Calendar tool (V1)
- `NSRemindersUsageDescription` — Reminders tool (V1)
- `NSFaceIDUsageDescription` — Biometric lock
- Keychain sharing group: none (device-local only)

---

## 6. Static audit result

```
python3 docs/spec/v3/20_VALIDATE_HANDOFF.py
STATIC AUDIT: 16/16 PASS
APP COMPILE: NOT_RUN; IPAD/IPHONE DEVICES: NOT_RUN; PROVIDER LIVE TESTS: NOT_RUN
exit code: 0
```

---

## 7. W00 gate evidence

| Field | Value |
|---|---|
| gate_id | W00 |
| environment_OS | Linux Ubuntu x86_64 |
| toolchain_version | Swift NOT_FOUND |
| command_exact | `python3 docs/spec/v3/20_VALIDATE_HANDOFF.py` |
| exit_code | 0 |
| test_case_IDs | static audit 16/16 |
| actual_result | all 16 doc/contract checks PASS |
| status | PARTIAL |
| remaining_P0_P1 | iOS SDK absent (Linux); no device; no credentials |
| next_exact_action | W01 — implement Domain/ typed contracts |

**iPad Playground import:** BLOCKED (no iPad device in this environment).  
**Portable Swift source:** UNBLOCKED — continue W01–W14 as portable source.

---

## 8. App target source mapping

All V1 source files will be placed under `PersonalAssistant.swiftpm/` (the `AppModule` target path is `.`). Subdirectories:

```
PersonalAssistant.swiftpm/
  App/
  Domain/
  DesignSystem/
  Persistence/
  AI/
  Security/
  Voice/
  Avatar/
  Tasks/
  Tools/
  Media/
  Search/
  Integrations/
  Features/
  Resources/
```
