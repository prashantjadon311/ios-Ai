# GEMINI: FINAL IMPLEMENTATION, VERIFICATION AND IPAD SHIPPING CAMPAIGN

**Use this as a single, persistent execution contract in the existing Antigravity VS Code workspace.** This is not a request for another plan, code generator, superficial source audit, or fictional PASS report. Implement and verify the existing V3 V1 scope against the actual repository. Where physical Apple hardware or credentials are necessary, prepare exact reproducible handoff and resume from real results; do not claim that a Linux session operated an iPad.

## ROLE AND OBJECTIVE

Act as principal iOS/Swift 6 engineer, SwiftUI and SwiftData integration engineer, AI inference/streaming engineer, security engineer, test engineer, and release engineer. The objective is a **genuinely working PersonalAssistant.swiftpm app**, importable and runnable in Swift Playgrounds on iPad with the five primary screens (Dashboard, Tasks, History, Configuration, Settings), supporting screens, separate configurable Maya/Saar assistants, user-owned Groq/OpenRouter/custom-provider streaming AI, local history/memory/tasks, notifications, working tap-to-talk/TTS on supported devices, and safe gated actions. The source should remain portable to Xcode and an independently distributed iPhone build, but **opening a .swiftpm in Playgrounds on an iPad does not by itself establish installability on iPhone**.

Current last-audited public baseline: GitHub `prashantjadon311/ios-Ai`, commit `5905602c5c504d1d6f9d3a140edc790b77c2cb2f` (Third). This is only an audit reference. Local HEAD and source may be newer. Discover the actual repository root with `git rev-parse --show-toplevel`; do not assume a fixed directory.

## AUTHORITY AND INPUTS

Read `CLAUDE.md`, then `docs/spec/v3/README_START_HERE.md`, `00_V3_AUTHORITY_AND_FINDINGS.md`, `12_FINAL_ENGINEERING_DECISIONS.md`, `01_RELEASE_SCOPE_AND_EXECUTION.md`, `13_EXACT_EXECUTION_AND_ACCEPTANCE.md`, `14_CRITICAL_ALGORITHMS_AND_RECOVERY.md`, `15_SECURITY_PRIVACY_BACKEND.md`, `02_CANONICAL_CONTRACTS.md`, `11_CORE_METHOD_BLUEPRINT.md`, `05_TEST_AND_SECURITY_MATRIX.md`, and the relevant per-file rows of `16_FILE_CONTRACT_INDEX.tsv` / `08_CANONICAL_MANIFEST.tsv`. Read remaining specifications only as needed for the subsystem you are implementing. The V3 documents' published precedence remains authoritative.

Read the two previously downloaded latest handoff files **if present** at `docs/implementation/audit/01_LATEST_REAUDIT.md` and `docs/implementation/audit/02_GEMINI_EXACT_NEXT_PROMPT.md`. If they are not in this local repository, report their absence, use the available `docs/implementation/audit/` reports, and revalidate concrete findings against local source; do **not** block implementation or hallucinate their contents. The actual files and test evidence outrank a prior assistant's summary. Any genuine inconsistency among authoritative V3 documents requires a brief recorded decision and the least-risk compatible implementation, not a silent redesign.

The V3 release table governs scope. Do **not** silently add cloud sync, multi-user authenticated backend, real Small AI model, always-listening background assistant, guaranteed autonomous jobs, or 3D avatars. Do not present local profiles as independent secure cloud accounts. Conditional Apple Foundation Models, newer Speech APIs, Spotlight, Calendar/Contacts and other integrations must stay truly feature-gated if platform or hardware support cannot be verified.

## EXECUTION STANDARD: DO THE WORK, DO NOT JUST DESCRIBE IT

Carry out one continuous implementation campaign. Treat W00–W14 as dependency and evidence gates, not separate opportunities to stop after producing plans. Each coherent code change must preserve source compatibility, add actual tests, and update the durable checkpoint. Prioritize a single **working vertical slice**, then complete V1 features; do not mass-generate dummy files to satisfy the file-count manifest. Never rerun old bulk generators over repaired files. Preserve existing working code, original `.swiftpm` manifest, user changes, and source history.

Start by running and recording: `pwd`, `git rev-parse --show-toplevel`, `git status --short`, `git rev-parse HEAD`, `git rev-parse origin/main`, current target/manifest inspection, source inventory and zero-byte list, and available SDK/toolchain commands. Inspect `docs/implementation/IMPLEMENTATION_CHECKPOINT.md`, `R0_SOURCE_RECONCILIATION.md`, `R1_SOURCE_TRUTH_AUDIT.md`, and all current test scripts. Record any newer source changes before touching files. Never use destructive `git reset --hard`, force push, or discard uncommitted work. Use a branch or safe local snapshot where appropriate.

**Known defects to reproduce or dismiss from actual local code, not blindly assume:** the audited Third commit still had empty `AI/Context/ContextBuilder.swift` and `MemoryProposalEngine.swift`; `AppContainer` called an `AssistantOrchestrator` initializer with a mismatched `configurationRepository` argument; `AdaptiveLayout` called view initializers inconsistent with actual screens; `ModelRouter` contained recursive/redeclared `id`, synchronous Keychain actor access, UUID/provider-ID mismatch and missing capability/privacy enforcement; `AssistantOrchestrator` produced fake start/completion; `ChatView` created a random ID for new chats, Dashboard discarded quick-ask input, and assistant messages were not durably persisted. Approval types used missing fields or optional security checks; receipts were kept only in RAM; key storage UI printed false success after `try?`; privacy UI diverged from persisted runtime policy; task occurrence keys were random; asset catalogs lacked Maya/Saar image files. Reconcile every one with current HEAD; some may have been repaired locally.

## REQUIRED REPAIR ORDER AND DONE CRITERIA

### A. REAL COMPILATION FOUNDATION (R1/W00–W03)

Create a canonical symbol/dependency ledger. Resolve all duplicate type declarations, invalid enum cases/labels, missing members/initializers, duplicate `@main`, bad imports, actor isolation, Sendable violations, resource paths and OS-availability guards. Consolidate dependency injection in the one AppContainer composition root. Keep exactly one owner of SwiftData `@Model` declarations and one stable schema/migration plan. Match `Package.swift` to the original iPad-generated template and preserve the `AppModule` target. Avoid `try?` or swallowed failures on critical storage/security paths. Don't simply make tests search for the corrected text.

Use genuine Swift type-check/build evidence where available. Host is likely Ubuntu without Apple SDK. Probe first. If it is unavailable, still implement portable source and executable pure-Swift tests if a compatible Swift toolchain is available. In parallel, prepare a **macOS GitHub Actions CI** job where project/runner/toolchain support permits a real iOS SDK build and simulator tests. Investigate the actual Apple-generated `.swiftpm` build commands rather than inventing `xcodebuild` flags. If CI cannot directly build the Playgrounds package, isolate shared Swift components for a legitimate Xcode test harness while preserving the canonical `.swiftpm`; record the app-target gap. Do not claim a shared-source-only compile is a full app build. Never publish or hardcode BYOK secrets into Actions, source, fixtures or logs.

### B. WORKING AI CHAT VERTICAL SLICE (R2/W05–W06)

Implement and actually connect: Dashboard text input -> conversation creation/selection -> ChatViewModel -> session-scoped persisted user message -> context assembly -> persisted owner-scoped provider config + real user's Keychain credential -> registered provider -> capability/privacy-aware ModelRouter -> actual OpenAI-compatible HTTP SSE stream -> orchestrator -> ordered visible response -> durable assistant message -> relaunch history. The Dashboard's exact submitted text must arrive in the chat and be sent **once**. New-chat navigation must not invent an unpersisted conversation ID. The UI must have visible loading, offline, invalid-key, rate-limit, model-unavailable, no-local-model, cancellation, interrupted and retry states.

Keep provider identity consistent: distinguish provider kind (`groq`, `openRouter`, custom) from config UUID, and bind each provider instance to the correct owner/key/config. Fetch/parse model catalogs when supported; do not assume hardcoded model IDs exist. Provider-specific tool/vision/stream capabilities must be verified or treated as unknown, not optimistically eligible. Validate base URLs, HTTPS and redirects. Propagate URLSession task cancellation and parse fragmented UTF-8/CRLF/multiline SSE, incomplete JSON, tool fragments and provider errors. Honor Retry-After with bounded retries and route health; no fallback after visible token/side effect, no fabricated completion after stream truncation. Persist interrupted content consistently and ensure one terminal event. Prevent double-saving the same assistant message in UI and orchestrator.

Privacy mode is a fail-closed network boundary, not a cosmetic setting: private-only must produce **zero outbound content network requests**, including provider fallback, attachment upload, URL previews, optional cloud speech and diagnostics. If no eligible local model is available, clearly show the limitation; do not route to cloud secretly. Secrets never enter model context. Direct BYOK budget warnings are advisory, not guaranteed provider billing caps. Require explicit user-owned keys; no developer-owned key in client.

### C. SECURITY, STORAGE AND RECOVERY (R3/W02/W09)

Before enabling a side-effecting tool, enforce current owner and session generation before and **after** suspension points, permission scope, risk, recipient, data classification, exact validated arguments and user authorization. Validate actual JSON Schema and tool-ID registry consistency. Canonicalize and hash exactly what the user reviewed (including owner, session generation, tool ID/version, destination, arguments and scope); require nonoptional current-session and expected-payload checks. Authorizations are single-use with expiry and concurrency-safe consume. Never trust an LLM/tool result as permission.

Build a **durable** SwiftData-backed receipt/operation ledger, not a dictionary. Atomically persist a unique PREPARED operation key before a side effect. On success persist receipt; on timeout/crash/unknown response classify AMBIGUOUS and require review or provider-specific idempotency reconciliation. Never blindly auto-retry an ambiguous external write. Unit-test simultaneous approvals, exact-payload mutation, account switch, expired approval, crash after PREPARED and no duplicate side effects. Keep unsafe external tools disabled until tests pass; implementing a no-op tool that returns 'Created'/'Opened' is forbidden. If an integration is not in shipping scope, disable it and explain why instead of falsely presenting it as working.

Fix Keychain error surfaces (locked vs missing vs write failure) and never show 'saved' until success. Prevent stale account/assistant callbacks from contaminating a new session. Switching assistants must update the activeAssistantID **without reconstructing/resetting all other preferences**. Persist privacy and permission settings and enforce them at the call site. For SwiftData migrations, failed opens preserve the original store, show recovery/export, and never silently reset local data. Audit deletion across database, attached files, caches and optional Spotlight. Treat local profiles as isolated within the app, not independently authenticated identities.

### D. FINISH REAL V1 FEATURES (R4/W04/W07–W12)

All five main screens and supporting Chat/Task editor/detail/Approval Center/Memory review/Provider setup/Diagnostics must have functional enabled controls. Both Maya and Saar need distinct **actual legally usable bundled avatar assets**, independent editable names/voice/preferences that persist on restart, visible listening/thinking/speaking states, accessible static fallback and Reduce Motion compliance. An SF Symbol circle alone does not satisfy the requested character-avatar deliverable; if genuine assets cannot be produced, mark the asset requirement blocked rather than done.

Voice: connect real audio capture to transcription and TTS, permission request UX, explicit Hindi/English locale selection, interruption handling, audio-engine and task cancellation, and avatar state. Don't label the legacy transcriber as a newer SpeechTranscriber implementation if it simply forwards to the legacy API. No unexpected cloud STT or always-on background recording. Require a real physical-device voice test.

Tasks: persist definition, revisions, runs, scheduled notification IDs, timezone/wall-clock recurrence and stable deterministic occurrence identity; implement save/edit/delete/schedule/cancel/relaunch reconciliation. Never use random UUID as the basis of duplicate prevention for the same scheduled occurrence. Scheduled notification firing != autonomous AI task execution. Show denied-notification and ambiguous-run states honestly. Test DST gap/overlap, clock/timezone changes, repeat task edits and restart.

Memory/history: verified memories with source provenance, correct owner and revision, retention and deletion; bounded context selection and actual retrieval rather than truncating arbitrary messages without summaries. Search real message content where specified, not only conversation titles. Media: enforce file limits, validated MIME/magic bytes, correct local storage lifetime, bounded PDF/OCR, no unapproved uploads, sandbox-safe filenames, cancellation and cleanup. Spotlight indexing should be opt-in, owner-scoped and removable; disable when not established safe.

Configuration/Settings: every enabled toggle writes durable state **and** changes real runtime behavior. Expose provider test and actionable error, model selection, privacy mode, voice, notifications, tool scopes and storage. Disable unfinished buttons with an honest explanation. Avoid false privacy statements: disclosures must match actual storage, networking, diagnostics and provider behavior. Ensure iPhone/iPad adaptive navigation, Dynamic Type, VoiceOver, keyboard, loading and empty/error states.

### E. REAL TESTS AND FIVE INDEPENDENT AUDIT PASSES (R5/W13–W14)

Convert T001–T028 and S001–S018 into **executable** Swift/XCTest/Swift Testing tests where supported, plus Apple-device manual cases. Keep Python validators if useful, but relabel them STATIC_CONTRACT_CHECK, not 'behavioral test'. Run tests against the actual Swift services using deterministic fake provider, fake Keychain, in-memory store, mocked URLProtocol and controllable clock/scheduler. Add failure-injection tests for partial SSE, 401/429/5xx, incorrect account, privacy egress, cancellation, crash after PREPARED, duplicate operation, missing model, data migration and notification denial. Test real Groq or OpenRouter only with a user-provided BYOK key, without logging it.

Run five independent review passes, repair findings after each, rerun impacted tests, and include exact evidence:
1. Architecture/contracts/dependency injection and full target compilation (not string matching).
2. End-to-end UI/data/AI behavior with deterministic integration tests and actual provider smoke.
3. Security/privacy: owner isolation, prompt injection, authorization, redirection, durable receipts, retention and egress fail-closed tests.
4. Device UX/performance/reliability: iPad Swift Playgrounds import/build, microphone, notifications, relaunch, rotate/split, offline, accessibility and memory/error states.
5. Packaging/release: assets, app capabilities, purpose strings, privacy manifest, no embedded keys, clean repo/CI results, reproducible handoff and independently gated iPhone build/install if requested.

Do not report an audit PASS on unrun tests. A static 46/46 means only source-pattern assertions passed; it does not override an observed missing orchestrator call or a compiler error. Keep a machine-readable test result ledger: `test_id | test_kind | command | OS/SDK | exit_code | evidence_path | PASS/FAIL/BLOCKED/NOT_RUN`.

## EXTERNAL DEVICE DEPENDENCIES AND HONEST STOP RULES

You cannot operate my iPad from this Ubuntu VS Code workspace unless I explicitly connect a supported tool. You **must** complete every independently executable task first, then provide the exact archive/package path, SHA-256, iPad import and run procedure, required Playgrounds capabilities/purpose strings, BYOK key-entry procedure, smoke checklist and a precise request for the *first actual iPad build diagnostics*. When I provide compiler/runtime logs, fix them in the same repo and reissue a new verified package. Do not call this fully iPad-verified before I run it or valid iPad evidence is supplied. For iPhone, document separately the signed TestFlight/App Store/Xcode distribution route as appropriate; iPad Playground execution alone is insufficient.

An uninstalled, ineligible or unavailable Apple on-device model must surface 'local AI unavailable' rather than be advertised as functional. Do not buy cloud services or publish to the App Store without explicit authorization. Continue unaffected modules while waiting for device inputs. No release-status claim of COMPLETE while required V1 gates have unverified or failed evidence.

## CHECKPOINT, TOKEN AND COMMIT DISCIPLINE

Do not consume the session on repeated full-document reads or verbose narration. Read canonical authority once, load relevant file contracts per gate, edit targeted files, and run tests. Every coherent gate: save a durable checkpoint under `docs/implementation/` with `HEAD`, actual changed paths, exact command and exit code, artifact/build SHA, tests, open P0/P1, blocking dependency and `NEXT_EXACT_ACTION`. Never rewrite older audit history to fake clean results; add a dated correction. Commit only tested coherent changes, do not force-push, do not stage secrets, and keep local uncommitted user changes intact.

When interrupted by model quota/context or an external dependency, write the checkpoint before stopping. On the next available execution, **resume from checkpoint and first unfinished boundary**, not W00. This prompt is intended to govern the whole remaining campaign, but cannot magically provide iOS tooling, device input, provider keys, or infinite model quota.

## FINAL DELIVERY CONTRACT

Produce actual corrected `PersonalAssistant.swiftpm` source and bundled assets, executable tests/fixtures, optional compatible macOS CI if viable, updated truthful V3/W00–W14 gate ledger, exact defect register, provider/Keychain and permission setup, privacy disclosure/third-party license review, final source SHA-256 and a one-page iPad import/run/smoke guide. For each required V1 feature state `IMPLEMENTED`, `SWIFT_TESTED`, `IOS_COMPILED`, `IPAD_TESTED`, `BLOCKED`, or `NOT_RUN`, with evidence. For iPhone state separately `NOT_INSTALLED`/`INSTALLED_AND_TESTED`.

**First real milestone:** actual type-consistent app plus one persistent live BYOK chat with verified no-cloud privacy routing. **Second:** secure tools, task, memory, voice, avatars and settings working. **Final:** a fresh iPad Playgrounds import and real device smoke; iPhone distribution verified separately when installed.

BEGIN NOW: recover HEAD, reproduce contradictions, correct foundational defects, implement the live AI vertical slice, execute real tests, and continue through the first genuinely blocked external-device gate. Do not stop after generating another audit report or plan.
